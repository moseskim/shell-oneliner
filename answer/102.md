### 해답
```
$ echo $BASH_COMMAND
```
### 다른 풀이
```
다른 풀이 1(야마다) $ Q () { type Q | sed '1d;4s/$/;/;$s/$/; Q/' | tr -d '\n' | tr -s ' ' | awk 4;}; Q
다른 풀이 2-1(야마다) $ bash: bash:: コマンドが見つかりません
다른 풀이 2-2(야마다) $ -bash: -bash:: コマンドが見つかりません
다른 풀이 3（上田）$ set -v
```
