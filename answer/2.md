### 해답
```
$ time ls *.png | sed 's/\.png$//' | xargs -P2 -I@ convert @.png @.jpg
```
### 다른 풀이
```
다른 풀이 1(야마다) $ time ls *.png | sed 's/\.png$//' | xargs -P$(nproc) -I@ convert @.png @.jpg
다른 풀이 2(야마다) $ mogrify -format jpg *.png
다른 풀이 3(야마다) $ time parallel 'convert {} {.}.jpg' ::: *.png
```
