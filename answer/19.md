### 해답
```
$ printf xxxx-xxxx 1<> cardno
```
### 다른 풀이
```
다른 풀이 1(야마다) $ IFS=-;a=($(<cardno));echo xxxx-xxxx-${a[2]}-${a[3]} > cardno
다른 풀이 2（上田）$ a=$(<cardno);echo xxxx-xxxx-${a:10} > cardno
다른 풀이 3（上田）$ ( read a ; echo xxxx-xxxx-${a:10} > cardno ) < cardno
```
