### 해답
```
$ grep '\.exe$' files.txt
```
### 다른 풀이
```
다른 풀이 1(야마다) $ cat files.txt | grep '\.exe$'
다른 풀이 2(야마다) $ cat files.txt | sed -n '/\.exe$/p'
다른 풀이 3(야마다) $ cat files.txt | awk '/\.exe$/'
다른 풀이 4(야마다) $ cat files.txt | perl -ne '/\.exe$/ and print'
```
