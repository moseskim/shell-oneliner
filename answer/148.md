### 해답
```
$ cat 27001.xml | xmllint --xpath '/ekidata/station/station_name/text()' -
```
### 다른 풀이
```
다른 풀이 1(야마다) $ grep -oP '<(station_name)>\K.*(?=</\1>)' 27001.xml
다른 풀이 2（上田）$ curl -s http://file.ueda.tech/eki/p/14.xml | grep -B1 京急本線 | grep -oE '[0-9]+' | xargs -I@ curl -s http://file.ueda.tech/eki/l/@.xml | grep station_name | sed 's/<[^<]*>//g' | awk '{print $1}'
다른 풀이 3（山田、上田）$ curl -s http://file.ueda.tech/eki/l/27001.json | jq | grep station_name | tr -d '[:print:]'
다른 풀이 4(야마다) $ curl -s http://file.ueda.tech/eki/l/27001.json | gron | grep -oP '.station_name = "\K.*(?=")'
```
